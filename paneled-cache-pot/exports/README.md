# exports

This directory is for generated meshes and migration validation outputs.

At the moment it contains only validation renders because the older workspace
did not include copied legacy export files for this project.
