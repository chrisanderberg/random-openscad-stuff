# exports

This directory contains:

- Legacy STL artifacts copied from the older flat `OpenSCAD` workspace.
- Validation renders written during migration checks, typically named
  `validation-*.stl`.
