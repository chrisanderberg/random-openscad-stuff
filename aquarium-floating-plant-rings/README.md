# Floating Plant Rings

Migrated from the older `Documents/OpenSCAD` workspace into the newer project
structure.

`models/` contains the source ring variants. `exports/` contains copied STL
artifacts from the older workspace for reference. Use `PROJECT.md` for context
and `REQUIREMENTS.md` for the active constraints.

## Quick checks

- Render all migrated source files with `tests/run-render-tests.sh`
- Override the OpenSCAD binary with `OPENSCAD=/path/to/openscad` if needed
