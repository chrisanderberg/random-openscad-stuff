# Render Matrix

## General checks
- [ ] `models/stand.scad` renders cleanly.
- [ ] `models/mount_ring.scad` renders cleanly using the shared module path.
- [ ] `models/mount_plates.scad` renders cleanly.
- [ ] Shared alignment values remain consistent across parts.

## Suggested cases
- Default stand.
- Default mount ring.
- Default mount plate.
- One alternate mount height or plate-length case if the project is revived.
