# Render Matrix

## General checks
- [ ] Each source file in `models/` renders in stable OpenSCAD.
- [ ] No zero-thickness rim or leg geometry appears in exported variants.
- [ ] Container-specific variants still preserve a solid supporting rim.

## Suggested cases
- Rectangle platform default parameters.
- Polygon platform default parameters.
- One container-specific variant per container family.
