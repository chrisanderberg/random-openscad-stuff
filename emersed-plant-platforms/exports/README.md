# exports

This directory contains:

- Legacy STL and 3MF artifacts copied from the older `Documents/OpenSCAD`
  workspace for migration verification.
- Validation renders generated during migration checks, typically named
  `validation-*.stl`.

Treat copied legacy artifacts as reference material, not as the source of
truth.
