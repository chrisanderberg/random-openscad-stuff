# models

Top-level source variants for the emersed plant platforms live here.

The current migration preserves the older flat set of source files. If this
project becomes active again, shared geometry may be extracted into
`lib/project/`.
