# Experiments

This directory is intentionally a sandbox. `models/` contains one-off
experimental `.scad` files and `exports/` contains copied reference meshes and
render outputs.

## Quick checks

- Render all experiment sources with `tests/run-render-tests.sh`
- Override the OpenSCAD binary with `OPENSCAD=/path/to/openscad` if needed
